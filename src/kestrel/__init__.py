"""kestrel root package.

Sub-packages ship across multiple splits. The mock site lives in
`kestrel.mock_site` and is the only sub-package wired in this split.
"""

from __future__ import annotations

__version__ = "0.1.0"
